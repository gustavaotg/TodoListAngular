import { HighlightCompletedTask } from './highlight-completed-task';

describe('HighlightCompletedTask', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedTask();
    expect(directive).toBeTruthy();
  });
});
